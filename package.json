{
  "name": "patina-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone WebGPU material painting studio and reusable engines",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "pack:libs": "node scripts/pack.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
